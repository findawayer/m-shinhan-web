/**
 * 애플리케이션 설정 모음입니다.
 *
 * @example 참조할 때의 구문은 다음과 같습니다.
 * import * as config from '~/config';
 * persistAuthentication(config.AUTHENTICATION_PERSISTED_DAYS);
 */

export const AUTHENTICATION_PERSISTED_DAYS = 90;

export const TOAST_MAX_VISIBLE_COUNT = 3;
export const TOAST_AUTOHIDE_TIMEOUT_MS = 3200;
